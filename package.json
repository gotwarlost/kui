{
  "name": "kui",
  "productName": "kui",
  "version": "0.1.0",
  "description": "Local kubernetes UI",
  "scripts": {
    "clean": "rm -rf vendor/ && (cd app && npm run clean)",
    "clean-build": "npm run clean && npm run build",
    "setup": "./setup.sh",
    "lint": "(cd app && npm run lint)",
    "test": "go test ./... && (cd app && npm run test)",
    "build": "dep ensure && go install ./... && (cd app && npm run build)",
    "watch": "(cd app && npm run watch)"
  },
  "keywords": [],
  "author": "gotwarlost",
  "license": "MIT"
}
